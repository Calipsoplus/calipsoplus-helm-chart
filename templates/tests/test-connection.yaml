apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calipsoplus-portal.fullname" . }}-test-connection"
  labels:
    {{- include "calipsoplus-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calipsoplus-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
