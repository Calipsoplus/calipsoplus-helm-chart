# Default values for calipsoplus-portal.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets:
  registry: eosc-pan-git.desy.de:5555
  username: johannes.reppin
  password: BYty9czkkfp1bAoibPSA
  email: johannes.reppin@desy.de
nameOverride: ""
fullnameOverride: ""

calipsoplus:
  oidc: 
    enabled: true
    client:
      id: calipsoplus-portal
      secret: '3f3ce8d7-b8f2-4f44-b4de-15776917ad1d'
      authEndpoint: '../auth'
      tokenEndpoint: '../token'
      userEndpoint: '../userinfo'
      jwksEndpoint: '../certs'
      signAlgorithm: 'HS256'
  redirectUrl: 'https://calipsoplus.desy.de/navigation'
  logoutUrl: 'https://calipsoplus.desy.de'
  docker:
    host: 'calipso-docker-host.desy.de'
    port: '2375'

guacamole:
  db:
    name:  guacamole
    mysqlUser: guac
    mysqlPassword: guacamole
    rootPassword: guacadmin
    image:
      repository: mysql
      tag: '5.7'

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
